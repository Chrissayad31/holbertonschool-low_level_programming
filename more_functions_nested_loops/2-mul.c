#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 *mul - function that multiplies two integers.
 *@a: first integer.
 *@b: second integer.
 *
 *Return: return (produit)
 */
int mul(int a, int b)
{
	int produit = a * b;

	return (produit);
}
