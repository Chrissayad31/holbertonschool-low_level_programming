#include <stdio.h>
#include "main.h"

/**
* print_rev - function that print a string in reverse
* 
* @str: integer value
*/
void print_rev(char *s)
{
         while (*s != '\0')
         {
                   _putchar(*str);
                   str--;
         }
         _putchar('\n');
}
